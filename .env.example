# Copy this file to `.env` and fill in the values you need.

# HuggingFace token, required only for private models or higher rate limits.
HF_TOKEN=

# Alternative path to the configuration file (optional).
LYRICSMITH_CONFIG=./config.yaml
